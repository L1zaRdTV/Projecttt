<Page x:Class="GenericStore.Pages.CategoriesAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="760"
      Title="CategoriesAdminPage">
    <Grid Background="#F6F7FB">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DataGrid x:Name="CategoriesGrid" AutoGenerateColumns="False" SelectionChanged="CategoriesGrid_SelectionChanged" FontFamily="Segoe UI" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdCategory}" Width="80"/>
                <DataGridTextColumn Header="Название категории" Binding="{Binding NameCategory}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <Label Content="Название категории" Foreground="#1F2937" FontFamily="Segoe UI"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox x:Name="CategoryNameBox" Width="300" Height="30" Margin="0,0,10,0" FontFamily="Segoe UI" TabIndex="50" MaxLength="50" ToolTip="Название категории" BorderThickness="0,1,1,1" Background="White" BorderBrush="#D1D5DB" Foreground="#1F2937">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <Button Content="Добавить" Width="120" Height="30" Margin="0,0,8,0" Click="AddButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#2563EB"/>
                <Button Content="Сохранить" Width="120" Height="30" Margin="0,0,8,0" Click="EditButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#2563EB"/>
                <Button Content="Удалить" Width="120" Height="30" Margin="0,0,8,0" Click="DeleteButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#2563EB"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
