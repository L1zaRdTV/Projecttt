<Window x:Class="GenericStore.Pages.UserEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пользователь" Height="440" Width="520" MinWidth="360" MinHeight="420"
        WindowStartupLocation="CenterOwner" Background="{StaticResource AppBackgroundBrush}">
    <Border Margin="16" Padding="18" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,14"/>

            <TextBlock Grid.Row="1" Text="Логин"/>
            <TextBox x:Name="UserNameBox" Grid.Row="1" Margin="0,22,0,10" Height="30" MaxLength="50"/>

            <TextBlock Grid.Row="2" Text="Email"/>
            <TextBox x:Name="EmailBox" Grid.Row="2" Margin="0,22,0,10" Height="30" MaxLength="100"/>

            <TextBlock Grid.Row="3" Text="Пароль"/>
            <TextBox x:Name="PasswordBox" Grid.Row="3" Margin="0,22,0,10" Height="30" MaxLength="50"/>

            <TextBlock Grid.Row="4" Text="Роль"/>
            <ComboBox x:Name="RoleBox" Grid.Row="4" Margin="0,22,0,10" Height="30" DisplayMemberPath="NameRole" SelectedValuePath="IdRole"/>

            <TextBlock Grid.Row="5" Text="Город"/>
            <ComboBox x:Name="CityBox" Grid.Row="5" Margin="0,22,0,10" Height="30" DisplayMemberPath="NameCity" SelectedValuePath="IdCity"/>

            <TextBlock Grid.Row="6" Text="Данные пользователя будут сохранены после нажатия кнопки." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,4,0,0"/>

            <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Width="110" Height="34" Margin="0,0,10,0" Click="CancelButton_Click"/>
                <Button x:Name="SaveButton" Content="Сохранить" Width="130" Height="34" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
