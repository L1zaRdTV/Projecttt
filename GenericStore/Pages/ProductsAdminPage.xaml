<Page x:Class="GenericStore.Pages.ProductsAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="760"
      Title="ProductsAdminPage">

    <Grid Background="#F4F4F2" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="ProductsGrid" Grid.Row="0" Margin="0,0,0,10" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionChanged="ProductsGrid_SelectionChanged" FontFamily="Segoe UI">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdCatalog}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Product}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Categories.NameCategory}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Descripton}" Width="200"/>
                <DataGridTextColumn Header="Изображение" Binding="{Binding PhotoPath}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Margin="0,0,10,0">
                <TextBlock Text="Название товара" Foreground="#242424" FontFamily="Segoe UI"/>
                <TextBox x:Name="ProductNameBox" Height="28" Margin="0,4,0,8" FontFamily="Segoe UI" TabIndex="100" MaxLength="100" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Категория" Foreground="#242424" FontFamily="Segoe UI"/>
                <ComboBox x:Name="CategoryBox" Height="28" Margin="0,4,0,8" DisplayMemberPath="NameCategory" SelectedValuePath="IdCategory" FontFamily="Segoe UI">
                    <ComboBox.Resources>
                        <Style TargetType="{x:Type ComboBox}">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#D6D3CE"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Foreground" Value="#242424"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ComboBox}">
                                        <Grid>
                                            <Border x:Name="Bd"
        CornerRadius="8"
        Background="{TemplateBinding Background}"
        BorderBrush="{TemplateBinding BorderBrush}"
        BorderThickness="{TemplateBinding BorderThickness}">
                                                <Grid>
                                                    <ContentPresenter Margin="8,0,28,0"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left"
                          Content="{TemplateBinding SelectionBoxItem}"
                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                                    <ToggleButton Width="24"
                      HorizontalAlignment="Right"
                      Background="Transparent"
                      BorderThickness="0"
                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                  Fill="#374151"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
                                                    </ToggleButton>
                                                </Grid>
                                            </Border>

                                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
       Placement="Bottom"
       AllowsTransparency="True"
       Focusable="False">
                                                <Border CornerRadius="8"
            Background="White"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="1"
            MinWidth="{TemplateBinding ActualWidth}">
                                                    <ScrollViewer Margin="4,6,4,6">
                                                        <ItemsPresenter/>
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#9CA3AF"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>

                <TextBlock Text="Цена" Foreground="#242424" FontFamily="Segoe UI"/>
                <TextBox x:Name="PriceBox" Height="28" Margin="0,4,0,8" FontFamily="Segoe UI" TabIndex="50" MaxLength="50" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Описание" Foreground="#242424" FontFamily="Segoe UI"/>
                <TextBox x:Name="DescriptionBox" Height="60" Margin="0,4,0,8" TextWrapping="Wrap" AcceptsReturn="True" FontFamily="Segoe UI" TabIndex="1000" MaxLength="1000" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Путь к изображению" Foreground="#242424" FontFamily="Segoe UI"/>
                <TextBox x:Name="PhotoPathBox" Height="28" Margin="0,4,0,8" FontFamily="Segoe UI" GotFocus="PhotoPathBox_GotFocus" TabIndex="100" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,10,0">
                <Button Content="Добавить" Width="150" Height="34" Margin="0,0,0,8" Click="AddButton_Click"
                        FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
                <Button Content="Сохранить" Width="150" Height="34" Margin="0,0,0,8" Click="EditButton_Click"
                        FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
                <Button Content="Удалить" Width="150" Height="34" Margin="0,0,0,8" Click="DeleteButton_Click"
                        FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
