<Page x:Class="GenericStore.Pages.OrdersAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="760"
      Title="OrdersAdminPage"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <Grid Background="#F4F4F2">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DataGrid x:Name="OrdersGrid" AutoGenerateColumns="False" SelectionChanged="OrdersGrid_SelectionChanged" FontFamily="Segoe UI" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding IdOrder}" Width="60"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding IdUser}" Width="100"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Data}" Width="150"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding StatusOrders.NameStatusOrder}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Label Content="ID" Foreground="#242424" FontFamily="Segoe UI"/>
                <Label Content="Дата заказа" Foreground="#242424" FontFamily="Segoe UI" Margin="70,0,0,0"/>
                <Label Content="Сумма" Foreground="#242424" FontFamily="Segoe UI" Margin="55,0,0,0"/>
                <Label Content="Статус" Foreground="#242424" FontFamily="Segoe UI" Margin="73,0,0,0"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox  x:Name="UserIdBox" Width="90" Height="30" Margin="0,0,8,0" FontFamily="Segoe UI" ToolTip="ID клиента" TabIndex="50" MaxLength="50" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="0"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <DatePicker x:Name="OrderDateBox" Width="140" Height="30" Margin="0,0,8,0" FontFamily="Segoe UI" DisplayDateStart="2026/03/30"/>
                <TextBox x:Name="OrderPriceBox" Width="120" Height="30" Margin="0,0,8,0" FontFamily="Segoe UI" ToolTip="Сумма" TabIndex="50" MaxLength="50" Background="White" BorderBrush="#D6D3CE" Foreground="#242424">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="0"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <ComboBox x:Name="StatusBox" Width="180" Height="30" Margin="0,0,8,0" DisplayMemberPath="NameStatusOrder" SelectedValuePath="IdStatusOrder" FontFamily="Segoe UI">
                    <ComboBox.Resources>
                        <Style TargetType="{x:Type ComboBox}">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#D6D3CE"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Foreground" Value="#242424"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ComboBox}">
                                        <Grid>
                                            <Border x:Name="Bd"
                           
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                                                <Grid>
                                                    <ContentPresenter Margin="8,0,28,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                                    <ToggleButton Width="24"
                                          HorizontalAlignment="Right"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="#374151"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                                                    </ToggleButton>
                                                </Grid>
                                            </Border>

                                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           Focusable="False">
                                                <Border
                                Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                MinWidth="{TemplateBinding ActualWidth}">
                                                    <ScrollViewer Margin="4,6,4,6">
                                                        <ItemsPresenter/>
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#9CA3AF"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
                <Button Content="Добавить" Width="100" Height="30" Margin="0,0,8,0" Click="AddButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
                <Button Content="Сохранить" Width="100" Height="30" Margin="0,0,8,0" Click="EditButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
                <Button Content="Удалить" Width="100" Height="30" Margin="0,0,8,0" Click="DeleteButton_Click" FontFamily="Segoe UI" Foreground="White" Background="#4B5563"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
