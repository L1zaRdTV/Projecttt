<Page x:Class="GenericStore.Pages.UsersAdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="1000"
      Title="UsersAdminPage">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Дважды нажмите на пользователя, чтобы открыть окно редактирования." Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="UsersGrid_MouseDoubleClick" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdUser}" Width="70"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding NameUser}" Width="*" MinWidth="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" MinWidth="170"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Roles.NameRole}" Width="150"/>
                <DataGridTextColumn Header="Город" Binding="{Binding Cities.NameCity}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" LastChildFill="False">
            <Button Content="Удалить выбранного пользователя" MinWidth="220" Height="34" Click="DeleteButton_Click" DockPanel.Dock="Left"/>
        </DockPanel>
    </Grid>
</Page>
